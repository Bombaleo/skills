# context-gatherer

Gathers and distills project context from a resources directory into
`.specwork/context.md`. Run by the orchestrator when the pipeline needs
project context collected before speccing begins.

**Tools:** Read, Glob, Grep, Write, Bash
