# object-modeler

Reads the prototype walk output from prototype-speccer (no second walk) and
produces `objects.md` — a catalog of every domain entity in the prototype:
attributes, lifecycle states, and state transitions. Runs in Stage 2,
immediately after prototype-speccer. Worker agent; not for standalone use.

**Tools:** Read, Glob, Grep, Write, Edit, Bash
