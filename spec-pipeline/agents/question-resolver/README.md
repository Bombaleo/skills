# question-resolver

Resolves open questions from all draft spec files against project context.
Answers what the context covers and surfaces what it cannot resolve so the
user can answer manually. Run by the orchestrator after all story files are
written.

**Tools:** Read, Glob, Grep, Write, Edit
